import React from 'react'
import { BrowserRouter, Route, Switch } from 'react-router-dom'

const Route = () => {
  ;<BrowserRouter>
    <Switch>
      <Route path='/list' />
      <Route path='/cadastro' />
    </Switch>
  </BrowserRouter>
}
